apiVersion: apps/v1
kind: Deployment
metadata:
  name: newssearch-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: newssearch
  template:
    metadata:
      labels:
        app: newssearch
    spec:
      containers:
        - name: newssearch
          image: haridasuavinash/newssearch:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev
            - name: SPRING_REDIS_HOST
              value: redis
            - name: SPRING_REDIS_PORT
              value: "6379"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: newssearch-app
spec:
  type: NodePort
  selector:
    app: newssearch
  ports:
    - port: 8080
      targetPort: 8080